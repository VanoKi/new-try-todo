export type TaskType = {
  addedDate: string;
  deadline: string;
  description: string;
  id: string;
  order: number;
  priority: number;
  startDate: number;
  status: number;
  title: string;
  todoListId: string;
};
